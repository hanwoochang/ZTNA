Mini ZTNA (Zero Trust Network Access) 시스템 요구사항 명세서
1. 프로젝트 개요
본 프로젝트는 **'결코 신뢰하지 말고, 항상 검증하라(Never Trust, Always Verify)'**는 제로 트러스트(Zero Trust) 보안 모델을 기반으로 한 마이크로서비스 아키텍처(MSA) 보안 인프라 구축을 목표로 합니다. 단순한 ID/PW 기반의 인증을 넘어, 사용자의 컨텍스트(기기 정보, 위치/IP 등)를 실시간으로 평가하여 내부망(Target Server)에 대한 동적 접근 제어를 수행합니다.

2. 시스템 아키텍처 구성 요소
본 시스템은 완벽한 망 분리와 역할 분담을 위해 4개의 독립된 노드(Node)로 구성됩니다.

Client Agent (모바일 앱): 사용자 인증 및 단말기 컨텍스트(Context) 수집.

Policy Server (정책 및 인증 서버): 신원 확인, 동적 위험도 평가, 출입증(JWT) 발급 및 로그 적재.

Gateway (ZTNA 프록시): 모든 트래픽의 중앙 통제, JWT 검증 및 내부망 라우팅.

Target Server (보호 대상 내부망): 외부 직접 접근이 원천 차단된 1급 기밀 서버.

3. 기능적 요구사항 (Functional Requirements)
4. 3.1 Client Agent (React Native)
FR-1. 단말기 정보 수집: 앱 실행 및 로그인 시 백그라운드에서 기기 고유 식별자(UUID/MAC)와 현재 연결된 네트워크의 IP 주소를 자동으로 수집해야 한다.

FR-2. 인증 요청: 수집된 컨텍스트 데이터(Device ID, IP)와 사용자 계정 정보(Email, Password)를 Policy Server로 전송해야 한다.

FR-3. 토큰 보관 및 통신: 발급받은 JWT를 안전하게 보관하고, 내부망 접근 요청 시 HTTP Header(Authorization: Bearer <token>)에 포함하여 Gateway로 전송해야 한다.

FR-4. 보안 대시보드 제공: 사용자의 현재 위험도 점수 및 시스템 접근 가능 상태를 시각적으로 제공해야 한다.

3.2 Policy Server (Node.js/Express)
FR-5. 사용자 관리: 사용자 회원가입 및 단방향 암호화(Bcrypt) 기반의 안전한 로그인 기능을 제공해야 한다.

FR-6. 컨텍스트 기반 동적 위험도 평가 (Dynamic Risk Scoring): 로그인 요청 시 아래의 규칙에 따라 위험도 점수를 산출해야 한다.

+50점: 필수 기기 정보 또는 네트워크 정보 누락 시

+30점: 등록되지 않은 새로운 기기에서 접근 시

+20점: 기존에 등록된 기기이나 평소와 다른 IP에서 접근 시

+100점: 관리자에 의해 차단/신뢰 해제된 기기에서 접근 시

FR-7. 접근 정책 결정 및 토큰 발급: 산출된 점수를 기반으로 접근을 통제한다.

0 ~ 29점 (안전): 로그인 허용 및 단기 유효 기간(예: 1시간)을 가진 JWT 발급.

30 ~ 69점 (주의): 토큰 발급 보류 및 추가 인증(Step-up) 요구 상태 반환.

70점 이상 (위험): 즉시 접근 차단(403 Forbidden).

FR-8. 보안 감사 로그(Audit Log) 적재: 모든 접속 시도에 대한 IP, 기기 정보, 산출된 위험도 점수, 최종 처리 결과(ALLOWED/DENIED 등), 상세 사유를 access_logs DB에 실시간으로 기록해야 한다.

3.3 ZTNA Gateway (Node.js/http-proxy-middleware)
FR-9. 트래픽 중앙 통제: Target Server로 향하는 모든 클라이언트의 요청은 반드시 Gateway를 거쳐야 한다.

FR-10. JWT 인가(Authorization) 검증: HTTP Header에 포함된 JWT의 위변조 여부, 서명(Signature), 유효기간(Expiration)을 엄격하게 검증해야 한다.

FR-11. 리버스 프록시(Reverse Proxy) 라우팅: 검증에 통과한 정상 트래픽만 Target Server로 포워딩하며, 검증 실패 시 즉시 차단(401/403) 및 에러를 반환해야 한다.

3.4 Target Server (보호 대상 내부망)
FR-12. 기밀 데이터 제공: 검증된 사용자에게만 내부망 데이터(예: 기밀문서, API 응답)를 제공해야 한다.

FR-13. 심층 방어 (Defense in Depth): Gateway를 거치지 않은 모든 무단 접근을 차단해야 한다.

4. 비기능적 / 보안 요구사항 (Security & Non-Functional Requirements)
SR-1. 우회 접근(Bypass) 원천 차단 (Localhost Binding): Target Server는 애플리케이션 및 OS 네트워크 스택 단에서 외부 통신을 차단하기 위해 리스닝 포트를 루프백(127.0.0.1 및 ::1)으로만 바인딩하여 논리적 망 분리(Microsegmentation)를 구현한다.

SR-2. 비밀번호 보호: 데이터베이스에 저장되는 모든 비밀번호는 평문 저장을 엄격히 금지하며, Bcrypt를 사용하여 솔트(Salt) 처리 및 해싱해야 한다.

SR-3. 무상태(Stateless) 인증 체계: 서버의 확장성과 성능을 고려하여 세션(Session)을 사용하지 않고, 디지털 서명된 JWT를 활용하여 무상태 인증을 구현한다.

SR-4. 환경 변수 분리: DB 접속 정보, JWT 시크릿 키 등 민감한 정보는 소스코드에 하드코딩하지 않고 .env 파일을 통해 시스템 환경 변수로 관리한다.

5. 데이터베이스 스키마 요구사항 (MySQL)
본 시스템은 ztna 데이터베이스 내에 다음 3개의 핵심 테이블을 구성하여 관계형 데이터를 관리한다.

users 테이블: 사용자의 이메일, 해싱된 비밀번호, 역할(Role) 등 핵심 신원 정보 저장.

devices 테이블: 제로 트러스트 검증을 위한 사용자별 단말기 정보(Device ID, OS), 신뢰 여부(is_trusted), 최근 접속 IP 기록.

access_logs 테이블: 모든 접근 시도에 대한 시스템의 동적 평가 이력(점수, 조치 결과, 사유, 시간)을 기록하는 감사(Audit) 로그 저장소.

6. 개발 환경 및 기술 스택
Backend: Node.js, Express.js

Frontend (App): React Native

Database: MySQL 8.x

Security & Auth: JSON Web Token (JWT), Bcrypt

Proxy: http-proxy-middleware

API Testing: Postman / Thunder Client
