Column Name,Data Type,Constraints,Description
id,INT,"PK, Auto Increment",기기 고유 식별자
user_id,INT,"FK, NOT NULL",소유 사용자 ID (users.id 참조)
device_identifier,VARCHAR(255),NOT NULL,기기 고유 UUID / MAC 등
is_trusted,BOOLEAN,DEFAULT TRUE,기기 신뢰 여부 (차단 시 FALSE)
last_ip_address,VARCHAR(100),,마지막 접속 IP
last_accessed_at,TIMESTAMP,DEFAULT CURRENT_TIMESTAMP,마지막 접속 일시
